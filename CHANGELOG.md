# Changelog

## Unreleased

## 0.1.0 - 2026-07-30

- Added deterministic recursive discovery with configurable anchored include and exclude patterns.
- Added versioned atomic state persistence and cross-platform process locking.
- Added queue-aware uploads, resilient HTTP retries, and injectable mock transports.
- Added resumable batch execution, permanent failure handling, and atomic source output writes.
- Added `run`, `resume`, and `status` CLI commands plus a public Python API.
- Added test and tagged GitHub Release workflows without automatic PyPI publishing.
